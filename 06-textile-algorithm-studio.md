# 06. 직물 알고리즘 공방

- 프로젝트: `textile-algorithm-studio`
- 작성일: 2026-09-15 / 상태: 구현 전 설계 / 우선순위: 6
- 대상: 초등 고학년~고등학교 수학·미술·정보 / 활동: 25분
- [공통 원칙](./00-shared-design-principles.md) · [목록](./README.md)

## 1. 질문·차별성

‘무늬를 반복하면 실제 천도 짤 수 있을까?’ 날실과 씨실의 위아래 규칙을 이진 격자로 만들고 입체 교차 구조를 확인한다.
기존 대칭 미술·규칙 찾기와 달리 규칙을 실제 연결 구조로 바꾸며, 표면 무늬와 직조 구조의 차이를 다룬다.
수학·미술·정보를 연결하고 작은 격자로 정확한 판정이 가능해 6순위다.

## 2. 학습 목표·오개념

- 0/1 규칙과 실의 위아래 관계를 대응한다.
- 반복 단위·색·직조 구조가 무늬에 각각 미치는 영향을 구분한다.
- 긴 뜀실 구간이 생기는 이유와 구조의 절충을 설명한다.
- ‘보기 좋은 이미지면 실제로 그대로 짤 수 있다’, ‘같은 표면색이면 같은 조직’을 방지한다.
- 물성 시뮬레이션 없이 강도·촉감을 확정하지 않는다.

## 3. 활동 흐름

1. 3분: 평직 사진형 삽화에서 위에 보이는 실 방향을 예측한다.
2. 5분: 4×4 이진 격자의 셀을 바꾸어 3D 교차를 확인한다.
3. 5분: 반복 단위를 늘리지 않고 줄무늬를 만드는 방법을 찾는다.
4. 7분: 긴 뜀실 경고가 있는 패턴을 수정하되 원하는 대각 무늬를 유지한다.
5. 5분: 격자·실 색·3D 결과를 함께 저장하고 규칙을 설명한다.
- 전이: 직조 격자는 고정하고 날실 색만 바꾸면 무엇이 변하는지 비교한다.

## 4. 화면·상태

| 화면 | 내용 | 행동 |
|---|---|---|
| 관찰 | 참고 이미지와 확대 | 규칙 예상 |
| 설계 | 반복 격자와 색 목록 | 셀 토글·행 복사 |
| 직조 | 3D 실 교차·단면 | 한 줄 짜기·회전 |
| 검사 | 뜀실 위치·길이 | 경고 구간으로 이동 |
| 결과 | 규칙·표면·설명 | 저장·내보내기 |

editing → weaving → inspected → saved. 실행 중 편집은 정지 후 적용한다.
셀 변경은 되돌리기/다시 실행을 지원한다. 크기 축소로 규칙이 사라지면 미리 표시하고 복구 가능하게 한다.

## 5. 모델·규칙

- 반복 격자 R은 2×2~8×8. 화면 전체는 반복된 24×24 교차점으로 제한한다.
- R[i,j]=1이면 날실이 위, 0이면 씨실이 위다. 행은 씨실, 열은 날실로 모든 화면에서 일치시킨다.
- 평직 R[i,j]=(i+j) mod 2. 대각 조직 예시는 R[i,j]=1 if (j-i) mod 4<2 else 0.
- 실 중심 좌표 간격 1 모형 단위, 교차점의 위/아래 높이 ±h, h=0.15 후보.
- 두 실의 중심 높이 차이가 시각 반경 합보다 커야 한다. 교차 사이 보간에서도 관통을 검사한다.
- 교차를 따르는 곡선은 구조 표현용이다. 실의 장력·마찰·압축은 계산하지 않는다.
- 뜀실 길이는 동일 실이 연속해 위 또는 아래에 놓이는 교차 수다. 반복 경계를 넘어 이어지는 구간까지 계산한다.
- 단위 전체가 한 상태인 행/열은 무한 반복 시 결속이 부족한 구조로 별도 경고한다.
- 최대 뜀실 4는 교육용 과제 제약이며 실제 직물의 보편적인 제조 한계가 아니다.

## 6. 데이터·평가

- Weave: id, repeatRows, repeatCols, matrix, warpColors, weftColors, version.
- Inspection: cyclicFloatRuns, unboundRows, unboundCols, geometryStatus.
- Challenge: targetPatternId, allowedRepeatSize, maxFloat, requiredSymmetry.
- 이미지의 유사도만으로 정답 판정하지 않는다. 격자 조건과 설정된 구조 제약을 판정한다.
- 자유 창작은 통과/실패 대신 사용한 규칙·경고 검토·설명 여부를 기록한다.

## 7. 3D·대체 보기

Three.js의 실 곡선과 인스턴싱을 사용하고 격자에서 선택한 교차점을 3D에서도 강조한다.
윗면·측면·교차 확대를 제공한다. 고정 카메라 버튼으로 길을 잃지 않게 한다.
WebGL 실패 시 SVG 위아래 교차도와 뜀실 표로 동일한 구조 검사와 편집을 유지한다.

## 8. 생성 이미지

- P0 직물 참고 12장+가상 적용 6장=18장. 전체 참고·가방·의상 적용 총 60~100장.
- 먼저 엔진으로 만든 평직/능직 기준 이미지를 참조해 제작한다.
- 예시: ‘첨부 직조 규칙의 색 배치를 유지한 직물 표면 참고 이미지, 평평한 조명, 과장된 장식 없음, 문자 없음’.
- 생성 결과가 격자 규칙과 정확히 맞지 않으면 정답 문제에 쓰지 않고 창작 참고로 표시한다.
- 검수: 실의 가짜 교차, 반복 경계, 색 수, 실제 전통 문양으로 오인할 명칭. 가상 디자인으로 명시한다.

## 9. 범위·단계

- P0: 평직/2:2 능직, 8×8 이하 편집, 두 방향 색, 뜀실 검사, 3D/2D, 저장.
- P1: 직기 리프트 플랜, 제한된 종광 구성과 제작 제약. 구조 검증을 추가한 뒤 도입한다.
- 제외: 산업용 직기 제어 파일, 실제 천 강도·촉감·생산 가능성 보증.
- M1: 이진 규칙·주기 경계 검사 → M2: 격자 UI → M3: 3D 교차 → M4: 검수 이미지 → M5: 미션 검증.
- 모듈: weaveModel.ts, floatInspector.ts, threadGeometry.ts, GridEditor.tsx, LoomScene.tsx, DesignReview.tsx. 각각 500줄 미만.

## 10. 검증·완료

- 평직의 모든 뜀실 길이는 1, 2:2 능직의 표준 반복은 2로 계산되어야 한다.
- 반복 경계 양끝의 연속 구간을 하나로 합치는 사례를 검증한다.
- 전부 1인 격자는 결속 경고, 크기 2와 8 경계, 색만 변경 시 구조 결과 불변을 검증한다.
- 행·열 표기와 3D 위아래가 전 교차점에서 일치해야 한다.
- 키보드 셀 이동·Space 토글, 320px 확대 편집, reduced-motion, 2D 모드 검증.
- gi-pulse는 검사 버튼에, 업데이트 내역은 상단 작은 버튼으로 제공한다. VoiceOver 제외.
- 완료: 규칙과 3D가 일치하고 반복 경계의 뜀실까지 검사하며 저장한 설계를 복원한다.
