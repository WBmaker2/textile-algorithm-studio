# 진행 기록

구현하면서 바꾼 것과 확인한 것을 남긴다. 수치·성능은 측정값이며 교육 효과
검증이 아니다.

## 2026-09-17 — P0 구현

- React 18 + TypeScript + Vite + Vitest + Three.js 정적 앱 골격.
- 엔진: 평직/2:2 능직, 셀 토글, 24×24 반복 전개, 주기 구간 병합, 뜀실 검사,
  실 교차 기하와 관통 판정.
- UI: 첫 화면(질문·예측·시작), 조직도 격자(키보드·Space), 24×24 표면, 교차
  확대·옆 단면·입체, 뜀실 검사, 저장·JSON 내보내기, 업데이트 내역.
- 검증: 테스트 31개, `tsc`·`eslint`·`vite build` 통과, 본 번들 251KB + 입체
  청크 547KB 분리.
- 화면 점검에서 잡아 고친 것: 표준 능직의 오경고(16개 알림 → 가장 긴 구간만
  표시), 390px 이하 구조 보기 넘침, range 입력 16px → 44px, 작은 라벨 대비
  3.96 → 4.83:1 이상, 조작 경계 3.13:1 이상, 경고 목록의 중첩 스크롤 제거.
- 대비 최저값: 텍스트 4.83:1, 조작 경계 3.13:1. 320·360·390·768·1280px 가로
  스크롤 없음.
- 남은 것: 저장 복원, 크기 축소 경고, 미션, 관찰 대조, 한 줄 짜기.

## 2026-09-17 — 배포 직전 다듬기

- 저장한 설계의 복원·삭제. 복원 전 `isValidMatrix`로 2~8 정사각 불리언 격자를
  확인하고, 복원은 되돌리기로 취소할 수 있다. `records.test.ts` 4개 추가.
- 반대 대각 능직 `twill22Mirror` 추가. 6 크기에서는 대각이 경계에서 이어져
  표준 뜀실 2가 되지 않음을 테스트로 고정(오류가 아니라 주기 구조의 결과).
- 미션 3종(평직·대각 능직·줄무늬)과 판정 이유 표시. `missions.test.ts` 4개.
- 관찰 패널: 엔진 가상 표본(실제 사진이 아님을 명시)과 시작 화면 예측의
  적중 여부, 1/0 규칙표 토글.
- 한 줄씩 짜기: 표면에서 짠 줄과 아직인 줄을 구분선과 함께 표시.
- 활동 단계 띠가 실제 진행(편집·구조 보기·검사·저장)에 따라 바뀐다.
- 반복 단위 축소 전 확인 대화상자. 이전 격자는 되돌리기로 복원.
- 입체 보기에 정면 복귀 버튼. JSON 내려받기는 더 이상 로컬에 저장하지 않음
  (저장과 내려받기 역할 분리).
- 배포 설정: `base: './'`(하위 경로 호환), 파비콘(평직 2×2), 설명 메타,
  `README.md` 작성.
- 검증: 테스트 41개, `tsc`·`eslint`·`vite build` 통과, `vite preview` 200
  확인, 상대 자산 경로(`./assets/…`) 확인.
- reduced-motion: `prefers-reduced-motion` 에뮬레이션에서 gi-pulse
  `animation: none`·정적 `opacity: 0.9`, 실행 중 애니메이션 0개 확인.
- 입력 지연: 셀 토글 클릭→DOM 반영 29.8ms(목표 100ms 내).
- 동작 검증: 단계 띠(관찰 완료→설계 진행→편집 후 직조 진행), 미션 3종 확인,
  반대 대각 인정, 축소 대화상자→2×2 + 되돌리기 가능, 한 줄 짜기 구분선,
  저장→수정→복원→삭제, 입체 정면 복귀 후 캔버스 유지.
- 화면: 데스크톱·모바일 재확인. 관찰·미션 패널 포함 전체 흐름 이상 없음.

## 배포 전 확인 목록

- [x] 테스트·타입·린트·빌드 통과
- [x] `vite preview`로 빌드 결과 제공 확인
- [x] reduced-motion에서 gi-pulse가 정적 테두리인지 확인
- [x] 일반 입력 반응 100ms 내 측정(29.8ms)
- [x] 데스크톱·모바일 화면 재확인
- [x] 공개 URL의 자산 경로와 기능 직접 확인(배포 후)
  - https://wbmaker2.github.io/textile-algorithm-studio/ 200, 제목·예측 3항·
    시작 버튼 비활성 상태까지 부팅 확인, JS/CSS 하위 경로 200
- [ ] 교과 관점의 단순화·오개념 검토(교사 검토, 별도 범위)

## 배포 기록 (2026-09-17)

- 저장소: https://github.com/WBmaker2/textile-algorithm-studio (공개)
- 커밋 `737f5c4` 푸시 → Actions `Deploy to GitHub Pages` 성공
- Pages 소스: GitHub Actions(workflow 빌드). 이후 `main` 푸시마다 자동 배포
