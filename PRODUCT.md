# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

delegated: React 18 + TypeScript + Vite, Tailwind CSS, Three.js, Vitest. 정적 웹앱으로 서버 없이 localStorage에 실험 기록을 저장한다. (P0 구현 승인)

## Users

초등 고학년~고등학교 학생이 수학·미술·정보 수업에서 25분 활동으로 사용한다. 교사가 활동을 진행하고 5분 정리에 활용하며, 질문 → 예측 → 실험 → 기록 → 재검증 흐름을 따른다. 공통 원칙상 VoiceOver·TTS·자동 재생 오디오는 범위에서 제외된다.

## Product Purpose

직물 알고리즘 공방은 0/1 이진 규칙을 실제 실의 위아래 교차 구조로 바꿔 보여주는 학습용 직조 실험실이다. 학생이 반복 격자를 편집하면 3D 실 교차가 즉시 바뀌고, 뜀실 검사가 구조적 문제를 알려준다. 성공은 질문 하나와 시작 버튼 하나로 진입한 한 학습 흐름(관찰→설계→직조→검사→결과)이 320~1280px에서 완결되는 것이다.

## Positioning

표면 무늬 이미지가 아니라 연결 구조 자체를 다룬다. 같은 표면색이라도 조직(날실/씨실 위아래 규칙)이 다르면 다른 구조임을 격자·교차·뜀실 검사로 증명한다. 기존 대칭 미술·규칙 찾기 도구가 구조를 이미지로만 보여주는 것과 달리 반복 경계를 넘는 뜀실까지 검사한다.

## Operating Context

교실 한 명교사 환경: 교사용 PC 프로젝터 또는 학생 개인 기기(태블릿·노트북). 25분 활동 5단계와 5분 정리. WebGL 실패 시 SVG 대체 보기로 동일 구조 검사를 유지한다. localStorage 실패 시 현재 세션과 JSON 내보내기를 제공한다.

## Capabilities and Constraints

- P0: 평직/2:2 능직 전환, 2×2~8×8 반복 격자 편집(셀 토글·행 복사), 두 방향 실 색 변경, 24×24 교차점 렌더, 뜀실 검사(반복 경계 포함), 3D/SVG 전환, 저장·복원.
- P1(이번 범위 외): 직기 리프트 플랜, 종광 구성, 제작 제약 검증.
- 제외: 산업용 직기 제어 파일, 실제 천 강도·촉감·생산 가능성 보증.
- 엔진은 렌더러와 분리된 순수 함수(weaveModel.ts, floatInspector.ts, threadGeometry.ts), 각 파일 500줄 미만.
- 결정 규칙: R[i,j]=1이면 날실이 위, 0이면 씨실이 위. 행은 씨실, 열은 날실. 평직 R[i,j]=(i+j) mod 2. 능직 R[i,j]=1 if (j-i) mod 4<2 else 0.
- 뜀실 길이는 동일 실이 연속으로 위/아래에 놓이는 교차 수, 반복 경계를 넘어 이어지는 구간까지 합산. 전부 1(또는 전부 0)인 행/열은 무한 반복 시 결속 부족으로 별도 경고.
- 최대 뜀실 4는 교육용 과제 제약이며 실제 직물 제조 한계가 아니다.
- 생성 이미지 18장(P0)은 엔진 기준 이미지 참조 제작이며 이번 구현 범위에서 제외한다. 참고 이미지 슬롯은 빈 상태 안내로 대체한다.

## Brand Commitments

- 밝은 한국어 UI. 첫 화면에 질문 하나와 시작 버튼 하나.
- 주요 화면은 미션·실험·비교·기록 구성. 320·360·768·1280px에서 흐름 검증.
- 중요한 다음 행동에 gi-pulse 아우라(한 번에 한 버튼, reduced-motion에서 정적 테두리).
- 색만으로 상태를 구분하지 않는다. 범례·텍스트·모양을 함께 제공.
- 드래그에는 숫자 입력·버튼·방향키 대체 조작 제공. 키보드 포커스와 다이얼로그 닫기/복귀 검증.
- 작은 '업데이트 내역' 버튼을 항상 찾을 수 있는 위치에 둔다.
- 앱 ID: textile-algorithm-studio. 기록 포맷: schemaVersion, appId, createdAt, scenarioId, parameters, seed, observations, prediction, explanation.

## Evidence on Hand

- 00-shared-design-principles.md, 06-textile-algorithm-studio.md (설계 문서 2개, 구현 전 상태)
- 코드·이미지 자산·테스트 없음. 생성 이미지와 실제 직물 사진은 미보유이며 미래 작업이 이를 발명해서는 안 된다.

## Product Principles

- 구조가 답이다: 표면 무늬가 아니라 위아래 교차 구조를 보여준다.
- 규칙을 고치면 세계가 바로 바뀐다: 격자 편집이 3D·검사에 즉시 반영된다.
- 모든 조작에 대체 경로가 있다: 드래그 대신 버튼·숫자 입력·키보드.
- 실패는 다음 단계를 알려준다: 뜀실 경고는 위치와 길이, 수정 힌트를 함께 제시한다.
- 수치는 목표가 아니라 설계 목표로만 기록한다: 실제 성능·교육 효과 검증과 구분한다.

## Accessibility & Inclusion

- 키보드 셀 이동·Space 토글 필수. 320px에서 격자 편집 가능. reduced-motion 대응.
- 색 외에 모양·텍스트로 상태 전달. 대형 터치 대상. VoiceOver는 범위 외로 명시적으로 제외.
